const html = document.querySelector("html");
const button = document.querySelector("input[name=theme]");

const intialColors = {
    
}

const changeColors = (colors) => {

}

button = document.addEventListener("change", ({target}) =>{

    target.checked ? changeColors(): changeColors()
})

